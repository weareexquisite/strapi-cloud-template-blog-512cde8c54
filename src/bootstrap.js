'use strict';

/**
 * The blog template's demo-data seeder lived here. Removed - content types
 * for SoccerTips.ai (glossary-term, guide, insight, static-page, author)
 * are defined in src/api/* and content is entered through the admin.
 *
 * The site reads via a read-only API token (created in admin: Settings ->
 * API Tokens), so no public-role permissions are granted here.
 */
async function main() {}

module.exports = main;
