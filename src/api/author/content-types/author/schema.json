{
  "kind": "collectionType",
  "collectionName": "authors",
  "info": {
    "singularName": "author",
    "pluralName": "authors",
    "displayName": "Author",
    "description": "One named author: David Shaw (brief 10.1). Real credentials, real photo, sameAs only to verifiable profiles."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "name",
      "required": true
    },
    "bio": {
      "type": "richtext"
    },
    "photo": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"]
    },
    "credentials": {
      "type": "text"
    },
    "sameAs": {
      "type": "json",
      "description": "Array of verifiable profile URLs for Person schema. Never invent entries."
    }
  }
}
